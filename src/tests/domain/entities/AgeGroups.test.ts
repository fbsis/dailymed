import { AgeGroups } from '../../../domain/entities/AgeGroups';

describe('AgeGroups', () => {

  describe('instantiation', () => {
    it('should exist', () => {
      expect(AgeGroups).toBeDefined();
    });
  });


  
}); 